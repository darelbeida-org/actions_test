on: [push]
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Job to test Actions
    steps:
      - name: hello world
        run: echo "Hello world Actions!"
      - run: pwd
  container_job:
    runs-on: ubuntu-latest
    container: debian:buster-slim
    steps:
      - run: echo "Inside docker."
      - run: ls && pwd
      - run: cd ~ && pwd && ls -l
      - run: cd /github/home && ls -l
      - run: pwd
      - run: cd /__w/ && ls -l
      - run: cd /__w/_temp && ls -l
